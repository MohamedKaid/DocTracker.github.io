if email == record[0]:
        #     sql = "UPDATE Dispatch SET email = ? WHERE email = ?"
        #     curs.execute(sql, (newEmail, email))
        #     conn.commit()
        # else:
        #     return "Wrong email input"